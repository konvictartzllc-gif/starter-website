[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "node src/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Volume: Add a Railway Volume in the dashboard and mount it at /data.
# Then set DB_PATH=/data/konvict.db in the service environment variables.
