name: Legacy Azure deploy (disabled)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Explain migration
        run: |
          echo "Azure deployment has been retired."
          echo "Use .github/workflows/deploy.yml to build the client and deploy worker-autumn-cherry-0533 to Cloudflare Workers + D1."
