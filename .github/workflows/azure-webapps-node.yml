name: Legacy Azure workflow (disabled)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Explain migration
        run: |
          echo "This repository now targets Cloudflare Workers."
          echo "Use .github/workflows/deploy.yml for the active deployment pipeline."
